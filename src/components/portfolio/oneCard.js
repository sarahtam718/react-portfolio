import React from 'react';
import Button from './button';

const OneCard = props => {
  console.log('OneCard props', props.data);
  return (
    <div>
      <h1>hello</h1>
    </div>
  );
};

export default OneCard;
